/**
 * Deep-link stub. Settings opens as an overlay from SettingsModalProvider;
 * this route exists so old /settings URLs still resolve, then get bounced
 * back to the previous page with the modal open.
 */
export default function SettingsPage() {
  return null;
}
